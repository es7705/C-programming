#if 0
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	char ch;
	// scanf 함수가 문자를 입력하는 과정
	/*char ch;
	int i;

	for ( i = 0; i < 3; i++)
	{
		scanf("%c", &ch);
		printf("%c", ch);
	}*/

	/*while (1)
	{
		scanf("%c", &ch);
		if (ch == '\n') break;
		printf("%c", ch);
	}*/

	return 0;
}
#endif