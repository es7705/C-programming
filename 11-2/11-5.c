#if 0
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	// scanf 함수 반환값 활용
	// scanf 함수는 반환값이 있는 함수이다.
	// 입력 문자의 아스키 코드 값을 출력 하는 프로그램
	int  res;
	char ch;
	/*while (1)
	{
		res = scanf("%c", &ch);
		if (res == -1)
		{
			break;
		}
		printf("%d ", ch);
	}*/

	/*res = scanf("%c", &ch);
	while (res != EOF)
	{
		printf("%d ", ch);
		res = scanf("%c", &ch);
	}*/

	return 0;
}
#endif